import styled from "styled-components";

const AuthStatusContainer = styled.div`
  position: absolute;
  z-index: 9;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  background: linear-gradient(to right, #fc5c7d, #6a82fb);
`;

export { AuthStatusContainer };
